# ============================================
# Development Dependencies
# Install with: pip install -r requirements-dev.txt
# ============================================

# Testing
pytest>=7.4.0
pytest-asyncio>=1.3.0
pytest-cov>=4.1.0
pytest-mock>=3.15.1
pytest-xdist>=3.5.0
httpx>=0.24.0

# Type Checking
mypy>=1.5.0
types-requests>=2.31.0
types-redis>=4.6.0.20241004

# Linting
ruff>=0.15.12

# Retrieval-quality harness (benchmarks/retrieval/) — A7
# yaml is currently a transitive dep of chromadb/langgraph, but listing
# explicitly so the harness's fixture loader doesn't break on a future
# minor bump that drops the indirect dependency.
pyyaml>=6.0
